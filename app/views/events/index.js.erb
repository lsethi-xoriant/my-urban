//$("#events").append("<tbody class='page'><%= escape_javascript(render(@events)) %></tbody>");
$("#tex").html("<%= escape_javascript render('filter_events') %>")
url = $('nav.pagination a[rel=next]').attr('href');
alert(url);
